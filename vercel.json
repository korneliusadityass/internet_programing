{
    "build": {
        "env": {
            "LD_LIBRARY_PATH": "/usr/lib"
        },
        "commands": [
            "apt-get update",
            "apt-get install -y libssl1.0-dev",
            "composer install --no-dev --optimize-autoloader"
        ]
    }
}
